#include <stdio.h>

main() {
   double num;
   
   printf("Digite um numero --> ");
   scanf("%lf", &num);
   
   
   for (; num >= 1; num /= 2);
   
   printf("O resultado da ultima divisao e: %lf\n", num);
   
   return 0;
}
